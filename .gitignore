# ── Node ─────────────────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
*.log

# ── Build output ──────────────────────────────────────────────────────────────
_src/dist/
_src/.vite/
chart/.vite/
js/chart/chart.js

# ── Sensitive / persistent data (НІКОЛИ не в git) ────────────────────────────
server/database.sqlite
server/candles.sqlite
server/sessions.json
server/settings.json
server/ssl/
uploads/

# ── Local dev ─────────────────────────────────────────────────────────────────
.env
.env.local
.env.*
.DS_Store
Thumbs.db
.claude/
.vscode/

# ── Бекапи баз (зберігаються локально, не в git) ─────────────────────────────
.backups/

# ── SSH ключ для деплою ───────────────────────────────────────────────────────
.ssh/

# ── pixel-agents (окремий VS Code extension — не частина сайту) ───────────────
pixel-agents/

# ── TradingView Charting Library (ліцензована, не для git) ────────────────────
charting_library/

# ── Тимчасові файли ───────────────────────────────────────────────────────────
*.tmp
*.swp
*.swo
.tileset-working/

# Auto Claude data directory
.auto-claude/

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/
